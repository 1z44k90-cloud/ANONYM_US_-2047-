# GOVERNANCE READY

The repository governance system is prepared on `docs/agent-development-system` for review and integration into `main`.

This file is a marker only; the authoritative rules remain in the control documents under `AGENTS/`.
